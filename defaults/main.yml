---
# defaults file for create-autoscaling-group
region                   :
availability_zones       :

lc:
  name                   :

asg:
  name                   :
  min_size               :
  max_size               :
  desired_capacity       :
  vpc                    :
  default_cooldown       :
  health_check_type      :
  health_check_period    :
  load_balancers         :
  tags                   :

sp:
  increase:
    adjustment_type      :
    adjustment           :
    min_adjustment_step  :
    cooldown             :

  decrease:
    adjustment_type      :
    adjustment           :
    min_adjustment_step  :
    cooldown             :

metric:
  scaleup:
    threshold            :
    alarm:
      period             :
      evaluation_periods :

  scaledown:
    threshold            :
    alarm:
      period             :
      evaluation_periods :
