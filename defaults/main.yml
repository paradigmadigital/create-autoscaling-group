---
# defaults file for create-autoscaling-group
region                       :
availability_zones           :
lc:
  - name                     :

asg:
  - name                     :
  - min_size                 :
  - max_size                 :
  - desired_capacity         :
  - vpc                      :
  - default_cooldown         :
  - health_check_type        :
  - health_check_period      :

sp:
  - increase:
    - adjustment_type        :
    - adjustment             :
    - cooldown               :
  - decrease:
    - adjustment_type        :
    - adjustment             :
    - cooldown               :

metric:
  - scaleup_threshold        :
  - alarm_period             :
  - alarm_evaluation_periods :
